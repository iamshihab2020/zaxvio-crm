{
  "name": "@hvac-saas/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:apply": "tsx scripts/apply-migration.mts",
    "db:migrate": "node -e \"console.error('db:migrate is disabled in this repo.\\n\\ndrizzle-kit replays supabase/migrations/meta/_journal.json, but 32 files there are hand-written and unlisted, and drizzle.__drizzle_migrations is empty — so it restarts from 0000 and dies on CREATE TYPE booking_status, a type that has existed for months.\\n\\nUse:  pnpm db:apply <filename.sql>\\n\\nRe-enable this only after re-baselining the journal.'); process.exit(1)\"",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "drizzle-orm": "^0.45.1",
    "postgres": "^3.4.8"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.9",
    "tsx": "^4.21.0"
  }
}
