// This file previously contained the `users` table.
// It has been replaced by Better Auth's `user` + `member` tables in auth.ts.
// Keeping this file to avoid breaking barrel exports — re-export auth tables.
export {};
