packages:
  - apps/*
  - packages/*

# pnpm 10 blocks postinstall scripts by default. Both are needed:
#   esbuild — used by tsx (API dev server) and drizzle-kit
#   sharp   — Next.js production image optimization
onlyBuiltDependencies:
  - esbuild
  - sharp
